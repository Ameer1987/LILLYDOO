{% extends 'base.html.twig' %}

{% block body %}
    <div id="wrapper">
        <div id="container">
            <a href="{{ path('addressbook_index') }}">Manage Addresses</a>
            <br/>
            <a href="{{ path('country_index') }}">Manage Countries</a>
            <br/>
            <a href="{{ path('city_index') }}">Manage Cities</a>
        </div>
    </div>
{% endblock %}

{% block stylesheets %}

{% endblock %}
